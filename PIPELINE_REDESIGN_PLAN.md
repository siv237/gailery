# Gailery — Что сделано и что осталось

Реальный порядок после всех изменений:
```
СКАН → ХЕШ → ДЕДУП+INGEST → EXIF → FACES → DESCRIBE → EMBED
```

Этот документ — сводка: что из планов сделано, что оказалось не нужно, что ещё предстоит.

---

## Сделано

### Этап 1: Архитектура пайплайна

- [x] **Порядок шагов**: EXIF → FACES → DESCRIBE → EMBED. Реализован в pipeline.py
- [x] **Faces независим от Describe**: faces.py сканирует все фото по `faces_done=0`, не зависит от VLM
- [x] **Счётчики прогресса**: правильные — faces по faces_done, describe по described, embed по embedded
- [x] **Faces x10 от describe**: faces_batch=600, опережение по лицам

### Этап 2: Describe с контекстом

- [x] **Agent-контекст**: vision_describe.py собирает имена, возраст, позиции, комментарии персон, семейные факты, дату, alias папки
- [x] **VLM не ставит faces_done**: только InsightFace
- [x] **VLM не перетирает faces_present**: когда faces_done=1 — оставляет от InsightFace; когда faces_done=0 — фоллбэк
- [x] **Минимальный промт без правил**: 4 строки, факты и имена, без «запрещено/обязательно»
- [x] **Промты через settings БД**: редактируются через UI, воркеры подхватывают, фоллбэк на дефолт
- [x] **Отдельная страница промтов** в интерфейсе
- [x] **Возраст детей**: вычисляется в коде, до 18 лет, в месяцах/днях для младенцев

### Этап 3: GPU арбитраж и MQTT

- [x] **acquire_gpu() / release_gpu()**: мьютекс с verify, stale lock cleanup
- [x] **request_gpu_gentle()**: для поиска, ждёт до 120с
- [x] **request_gpu_for_api()**: жёсткий захват для enrich
- [x] **Pause/Resume pipeline** через MQTT control топики
- [x] **Watchdog**: убивает сирот llama-server, перезапускает pipeline

### Этап 4: Каскад инвалидации

- [x] **invalidate_for_persona()**: сброс described=0, embedded=0, удаление эмбеддингов
- [x] **Описание НЕ удаляется**: только флаги, pipeline переописал
- [x] **faces_done не сбрасывается**
- [x] **Вызывается при**: update_persona (rename), merge_personas

---

## Оказалось не нужно / неверно в планах

- ~~**exif.py исключает видео — это баг**~~ → Это правильно. У видео ffprobe, отдельный путь. Не надо смешивать
- ~~**VIDEO_EXTS унифицировать в config.py**~~ → Локальные наборы в каждом файле — осознанная изоляция под разные потребности. Единый список не нужен
- ~~**При изменении персоны удалять description=NULL**~~ → Абсурд. Описание остаётся читабельным, pipeline просто переописал. Только флаги
- ~~**SQL JOIN вместо N+1 в exif.py**~~ → Работает и так, не критично
- ~~**control_reset describe сбрасывает faces_present=0**~~ → Уже неактуально: VLM не ставит faces_done

---

## Что ещё надо сделать

### 1. Синхронизировать describe.py (Ollama-путь)

`describe.py` для Ollama-режима использует старый минимальный контекст (без возраста, семейных фактов, rich context). Надо перенести `_build_agent_context` или его аналог в Ollama-путь. Пока local и Ollama делают описания разного качества.

**Файлы:** `describe.py` (строки 136-173 — старые `_get_face_context`, `_get_photo_context`)

### 2. Удалить мёртвый код

В `vision_describe.py` остались старые `_get_face_context` и `_get_photo_context` — не вызываются, заменены на `_build_agent_context`.

### 3. Обновить AGENTS.md

Сейчас описывает старую версию. Нужно синхронизировать с текущим PIPELINE.md.

### 4. Stale-эмбеддинги (низкий приоритет)

`_mark_stale()` не удаляет векторы из LanceDB при устаревании. На практике не критично — следующий проход Dedup почистит.
